spring:
  application:
    name: discovery-service
  config:
    import: configserver:http://localhost:8888
 # security:
  #  oauth2:
   #   resourceserver:
    #    jwt:
     #     issuer-uri: http://localhost:8085/realms/football-realme
    #  client:
     #   registration:
      #    discovery-service:
       #     client-id: discovery-service
        #    client-secret: 21MXRgHkklmiVL1dY991mrwdG8K0KpmB # le secret récupéré
      #   authorization-grant-type: client_credentials
       #     scope: openid
        #provider:
        #  keycloak:
         #   token-uri: http://localhost:8085/realms/football-realme/protocol/openid-connect/token

server:
  port: 8761
